# Handoff — 3D takeoff view branch (for the next session)

Branch: `feat/3d-takeoff-view`, pushed to origin (knmurphy fork) @ `987fc7f`, clean tree.
Worktree: `.claude/worktrees/3d-view-impl`. Main checkout has the user's own dirty files — untouched.
Standing directives: **NO PR / no merge to main until the user explicitly approves** ("we'll pr when I approve it"); full dev pipeline for features (spec → adversarial 3-persona cycles until PASS×3 → plan → adversarial plan review → subagent TDD → headed validation → diff review); `npm run check` gate with `PATH="$HOME/.nvm/versions/node/v24.12.0/bin:$PATH"` in `web/`.

## Shipped on the branch (all verified headed, reviewed, pushed)

1. **3D takeoff view** (core) — scene3d.js pure builder, View3D.jsx overlay, W shortcut.
2. **Plan-skin ground plane** (r2) — sheet raster under geometry; `01ac9a2`.
3. **Roll-good lanes** (r3) — lane bands (material palette, clipRing footprint, odd-lane parity + single-lane exception) + seam ink at coverage boundaries; `b302c20`.
4. **Picking, labels, finishes, grid, studio look** (r4) — click-to-select via merged-mesh shapeRanges raycast (whitelist + visibility chain; roll meshes indexed → never carry ranges), floating label chip (units-aware, nominal rule), manufacturer finish textures (uvPlanar world/period, RepeatWrapping, session-only), 1/10 ft grid + cobalt/slate axes, Environment section (Backdrop/Pastel/Edges/Grid), export PNG composites the real backdrop both themes; `3bfd1b8`+`fb8b59e`+`987fc7f`. Also fixed pre-existing bug: destructive keys (Backspace/⌘Z/Esc) used to fire under the open overlay (invisible delete, camera refit) — now gated.

Spec = `docs/superpowers/specs/2026-08-26-3d-takeoff-view-design.md` (addenda r2/r3/r4 rev 3), plans alongside. Owner rulings recorded: openings are NOT a fidelity gap (LF already breaks at doors; continuous wrap-through is correct — `463400b`); gaps_norm slot stays presentation-only future work. Detail-view 3D texture stays a non-goal (fidelity read was positive).

## State right before reboot

- Dev server `dev3d` (hub-managed, restart on failure) serves the worktree at **localhost:5173** — restart it if the reboot killed it: `hub start` cwd `.claude/worktrees/3d-view-impl/web`, npm run dev.
- Performance + bug audit of roll lanes came back clean (100-room stress ≈0.7 ms pure; 60 fps orbit; fuzz 0 failures).
- Nothing mid-flight. All cycles closed: r4 diff review's one Important finding (selection effect missing texState dep) is fixed and live-verified.

## Open candidates (user's call, in rough order)

- Pastel strength (0.35) / band alpha (0.25) / seam width — one-line constants if the read feels off after user hands-on.
- Point-in-polygon selection isolation for unlinked shapes (spec future work).
- Plan-control persistence across opens (trivial).
- Texture persistence in .otk (explicitly deferred — r4 ruled runtime-only v1).
- Fog / contact shadows (ruled non-goals v1, revisit only if asked).
- PR — ONLY when the user approves.

## Tooling gotchas that cost time this session

- Headless CDP `page.mouse.click` does NOT produce PointerEvents — drive 3D clicks with synthetic `PointerEvent` dispatch on the canvas.
- The overlay close button is `title === 'Close'` (the `×`) — loose `/close/i` matches "Close this PDF" gallery buttons behind the overlay and can delete PDFs / hang on confirms. Use `dialogs:"dismiss"` (never accept — accepts delete confirms).
- Right sidebar "close" flakiness: several near-identical buttons; always select by exact title.
- Fiber walking: View3D's three objects live in `useRef` hooks — read via hook-node `.memoizedState.current` (Map of Groups) from any DOM node's `__reactFiber`; the renderer itself is a local const (unreachable).
- `darkMode` (sheet negative print, localStorage `opentakeoff_dark`) ≠ chrome theme (`opentakeoff_theme`, what `isDark` must read — TakeoffCanvas ~390).
- Fixture: `web/test/fixtures/3d-view-test.otk` (regen: `node test/fixtures/make-3d-test-project.mjs`); covers all 3D cases incl. RC-1 roll rooms (single-lane + L-shaped).
- Scene3d suite: 41 tests (`cd web && node --import tsx --test test/scene3d.test.ts`).

Full verification records: `.superpowers/sdd/2026-08-26-3d-takeoff-view/progress.md` (untracked by design).
